{
  "name": "clayton",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://claytongentry.github.io",
  "scripts": {
    "start": "python3 -m http.server 3000 --directory public",
    "build": "rsync -a --delete public/ build/ && cp CNAME build/",
    "deploy": "npm run build && git subtree push --prefix build origin production"
  }
}
